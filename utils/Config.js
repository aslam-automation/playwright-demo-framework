export const config = {
  // Base URL for API requests — override with environment variable API_BASE_URL
  // Public sample API is used by default so the example runs without a local backend.
  baseURL: process.env.API_BASE_URL || 'https://jsonplaceholder.typicode.com'
};

